apiVersion: v1
kind: Service
metadata:
  name: elt-pipeline-trino
  labels:
    app.kubernetes.io/name: elt-pipeline
    app.kubernetes.io/component: trino-serving
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: elt-pipeline
    app.kubernetes.io/component: trino-serving
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
