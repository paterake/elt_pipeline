# TODO Tracker

This file is the top-level tracker for backlog and review documents under `docs/todo/`.

Use it to see which backlog documents exist, what each one is for, and whether it is still active.

## Current Status

- Active implementation backlog documents: **0** (all approved scope complete or actively rejected). Serving-layer workstream under [PRD 09](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/09-prd-level3-level4-serving-and-table-format.md) closed S10 Workstation 2026-08-17: Phase 1 COMPLETED (L3/L4 Iceberg + Trino JDBC end-to-end), Phase 2 REJECTED (L2 Iceberg = wrong approach for a source-aligned intermediate layer; thin external catalog overlay preferred if trigger fires).
- Archived implementation backlog documents: **3 new archives (2026-08-17)** — L3/L4 Iceberg serving cutover (COMPLETED S10), L2 Iceberg lake proposal (REJECTED), L3/L4 serving session history snapshot. Added to prior archives: Custom-Code → Spark-Native Execution Parity (platform config-cascade and write-protocol alignment), URI-aware storage root paths + I/O dispatch, local-first implementation slice, Spark-only engine correction, path/partition management post-Spark migration, pure-Spark normalize cutover, publish-sink Spark parity resolution. All prior items COMPLETED or COMPLETED pending environment sign-off.
- **Closed workstream (2026-08-17 — S10 Workstation JDK 23):** **Serving layer — L3/L4 consumable via Apache Iceberg + Trino JDBC BI-agnostic endpoint → COMPLETE.**
  - Phase 1 (L3/L4): [archive/TODO_L3_L4_ICEBERG_SERVING_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_L3_L4_ICEBERG_SERVING_COMPLETED.md) — Row 1 F-3 zero-env 6-step proof (INGEST RC=0, NORMALIZE RC=0, SQL ICEBERG 4 models RC=0, PUBLISH CSV RC=0). GATE I3 REGISTER ×4 RC=0, L3/L4 SELECT A-100/A-200 2 rows visible via Trino JDBC. GATE I5 parity 4/4 MATCH row_count + md5 identical parquet↔iceberg. Row 4 publish RC=0 (namespace=iceberg ×3 DatasetRefs, L5 CSV/JSONL/TSV 2 rows, 2× audit JSON serving_endpoint populated). Row 5 OD-I1 step (a) default flip opt-in→opt-out complete: `_iceberg_effective_enabled` floor `return True`; 2× argparse `--no-iceberg-enabled` paired flags. Backlog Integrity Check 21/21 ANCHOR tags confirmed. RUFF `src/` tree 0 errors.
  - Phase 2 (L2 Iceberg): [archive/TODO_L2_ICEBERG_LAKE_REJECTED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_L2_ICEBERG_LAKE_REJECTED.md) — REJECTED WRONG APPROACH FOR L2 (2026-08-17). 5-point rejection rationale: (1) L2 is source-aligned staging, Iceberg snapshot guarantees irrelevant, (2) glob deletion solvable 10× cheaper via thin external catalog overlay (100 LOC registrar, no migration), (3) L3/L4 Iceberg is where consumer guarantees live, extending to L2 is second-system uniformity, (4) Jupyter public API 2-import template closes developer exploration DX without new infra, (5) OD-L2-1/2 have no resolution better than status quo `run_id=*` immutable dirs. Decision rule: when trigger fires, BUILD THIN EXTERNAL CATALOG OVERLAY FIRST (2 sprints dev soak) → reassess. Expect L2 Iceberg migration never happens.
- **New remaining operator scope (no backlog — next cycle triggers):**
  - OD-I1 step (b): Delete `sql/_staging_swap.py` L3/L4 legacy staging-swap path after soak period (Row 5 depends_on all GREEN).
  - Trigger conditions for L2 catalog: L2 scale pain OR real L2 JDBC/BI consumer → implement thin external catalog overlay (100 LOC registrar), NOT L2 Iceberg.
- **Completed milestone (2026-08-15):** **All-Spark platform parity COMPLETE.** No remaining custom-Python data-plane sites. Pure-Spark normalize cutover **Gates C1–C4 ARCHIVED as COMPLETED 2026-08-15**. Gate C4 (publish sink OD-P1) resolved via Option 3 streaming (`toLocalIterator()` replacing `collect()`) + size guardrail (`ELT_PIPELINE_PUBLISH_MAX_ROWS` default 1M rows, fail-fast). `result_df.collect()` removed from last data-plane site at [publish/runtime.py](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/publish/runtime.py); code-commented as accepted sink boundary per OD-P1 (single-file + checksum delivery contract is intrinsic and cannot be Spark-parallelized anyway).
- **Prior completed workstream (2026-08-15):** **Publish Sink Spark Parity Resolution (OD-P1).** Replaced driver `result_df.collect()` (full-set heap spike) with `toLocalIterator()` (partition-by-partition streaming) in [runtime.py](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/publish/runtime.py). Added config-driven row-count ceiling (`ELT_PIPELINE_PUBLISH_MAX_ROWS` env var, default 1,000,000) with fail-fast error code `PUBLISH_ROWS_EXCEED_CEILING`. Refactored `_validate_publish_output` to use Spark-side `row_count` (no row iteration needed). Refactored `_write_publish_output` to accept `Iterator[Row]` and return `rows_written`, with a Spark-count-vs-written mismatch sanity guard (`PUBLISH_ROW_COUNT_MISMATCH`). Added explicit sink-boundary code comment at call site (Option 1 documentation). Verified: byte-identical csv/tsv/jsonl output preserved; checksum + row_count + packaging semantics unchanged.
- **Prior completed workstream (2026-08-15):** **Pure-Spark Cutover: Retire the Custom-Python Normalize Engine** → see [archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md). Gates C1–C3 COMPLETED 2026-08-15. Gate C4 (publish sink OD-P1) = now COMPLETED 2026-08-15 (see entry above).
- **Prior completed workstream (2026-08-15):** **Custom-Code → Spark-Native Execution Parity** — Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates + archive move COMPLETED 2026-08-15; Gate 5 environment sign-off pending (now unblocked). Built the Spark-native relationalizer + platform-native staging-swap write protocol. See [archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md).

## Backlog Index

| Document | Type | Status | Purpose |
| --- | --- | --- | --- |
| `docs/todo/archive/TODO_L3_L4_ICEBERG_SERVING_COMPLETED.md` | **Completed + archived implementation backlog** | **Archived COMPLETED 2026-08-17 S10 Workstation. Rows 1–5 DONE P0/P1, BACKLOG-INTEGRITY-CHECK 21/21 ANCHORS, RUFF 0 errors.** | **L3/L4 Iceberg serving layer — make platform consumable → COMPLETE.** Phase 1 of PRD 09. Final state: (1) P0 Row 1 zero-env end-to-end 6-step F-3 proof (INGEST→NORMALIZE→SQL ICEBERG 4 models→PUBLISH all RC=0), (2) P0 Gate I3 Trino register_table ×4 RC=0 + L3/L4 SELECT 2 rows via JDBC, (3) P0 Gate I5 parity 4/4 row_count + md5 match parquet↔iceberg, (4) P0 Row 4 publish namespace=iceberg + CSV/JSONL/TSV L5 + audit JSON serving_endpoint populated, (5) P1 Row 5 OD-I1 step (a) default opt-in→opt-out flip: `_iceberg_effective_enabled` floor `return True` + `--no-iceberg-enabled` paired argparse flags on `sql run` + `publish run`. Operator next step: OD-I1 step (b) delete `sql/_staging_swap.py` legacy path after soak period. See session block inside doc for complete blocker bug table (13 resolved) + evidence. |
| `docs/todo/archive/TODO_L2_ICEBERG_LAKE_REJECTED.md` | **Rejected + archived proposal backlog** | **Archived REJECTED 2026-08-17 S10 Workstation. WRONG APPROACH for source-aligned staging.** | **L2 Iceberg lake proposal → REJECTED not deferred.** 5-point rejection rationale: (1) L2 contract = source-aligned staging, Iceberg snapshot/spec guarantees irrelevant, (2) `path_rglob + mergeSchema` glob deletion solvable 10× cheaper via thin external catalog overlay (~100 LOC registrar hook, no migration, no data file rewrite), (3) L3/L4 Iceberg is where table-format value accrues for consumers; L2 extension = second-system uniformity, (4) Jupyter + 2-line builder public API closes developer exploration DX with zero infra, (5) OD-L2-1/2 open decisions have no answer better than status-quo `run_id=*` immutable dirs. Decision rule for trigger: L2 scale pain OR L2 JDBC/BI real consumer → FIRST implement thin external catalog overlay (100 LOC registrar), 2-sprint dev soak → reassess. Only reopen THIS backlog if overlay genuinely fails (not expected). Gates L2-1 through L2-4 preserved in the archived document for completeness if reopen happens against expectation. |
| `docs/todo/archive/TODO_L3_L4_ICEBERG_SERVING_HISTORY_2026-08-17_COMPLETED.md` | **Session-history snapshot (archived)** | **Archived 2026-08-17 S10 Workstation. Snapshot of L3/L4 doc state pre-closure + session narrative appendix.** | **L3/L4 serving workstream session history snapshot.** Point-in-time copy of the Phase 1 backlog taken at closure for cold-start continuity / historical audit of intermediate re-verification states (2026-08-16 7-session independent re-verification). No active work here; for current completion state, see `TODO_L3_L4_ICEBERG_SERVING_COMPLETED.md` above. |
| `docs/todo/archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md` | **Completed + archived backlog** | **Archived COMPLETED 2026-08-15. Gates C1–C4 DONE.** | **Pure-Spark cutover: retire the custom-Python normalize engine — COMPLETE.** Platform normalize goal achieved: all L1→L2 relationalization is now pure Spark. Execution: **Gate C1** row-level parity on Temurin 17.0.20 — 2/2 row-level tests + 7/7 post-C3 Spark-native parity tests PASS. 2 bugs found & fixed in `spark_runner.py` (array-column projection drop; `posexplode_outer([])` sentinel filter). Full suite: 212 passed, 32 failed (all 32 pre-existing unrelated drift). **Gate C2:** default flipped to `spark`; `--normalize-engine` reversible flag + operator runbook updated. **Gate C3:** `runner.py` and `test_normalize_runner.py` DELETED; `pipeline.py` rewrote to remove `NormalizeEngine` switch, Python branch, `normalize_engine`/`normalization_runner` params; `cli.py` removed the flag and param threading; `level2_storage.py` deleted `_rows_to_dataframe()` + `write_table()` (Python-only path); `tests/test_normalize_engine_parity.py` rewrote from runner-vs-Spark to unconditional Spark-native assertions. Dead-path `grep` on `src/`+`tests/` for `NormalizationRunner|normalize_engine|_rows_to_dataframe|NormalizeEngine` = **0 matches**. **Gate C4 (publish sink OD-P1): RESOLVED 2026-08-15 per [archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md) — Option 3 streaming + size guardrail.** |
| `docs/todo/archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md` | **Completed + archived backlog** | **Archived / COMPLETE 2026-08-15. OD-P1 RESOLVED (Option 3 streaming + guardrail).** | **Publish sink: driver `collect()` → `toLocalIterator()` streaming + size guardrail — RESOLVED + ARCHIVED.** The last remaining custom-Python data-plane site. **Decision (OD-P1): Option 3 combined with Option 1 documentation + size guardrail.** (a) Replaced full-set driver `collect()` with `result_df.toLocalIterator()` (partition-by-partition streaming; eliminates driver-heap spike). (b) Added Spark-side `.count()` pre-check against config-driven ceiling `ELT_PIPELINE_PUBLISH_MAX_ROWS` (default 1,000,000) → fail-fast `PUBLISH_ROWS_EXCEED_CEILING`. (c) Refactored `_validate_publish_output` to use Spark-side `row_count:int` (no row iteration); `_write_publish_output` accepts `Iterator[Row]` and returns `rows_written` with `PUBLISH_ROW_COUNT_MISMATCH` sanity guard. (d) Added accepted-sink-boundary code comment at call site (single-file + checksum delivery is intrinsic; Spark's `df.write.*()` = `part-*` dirs = wrong shape for external-consumer artifact). csv/tsv/jsonl byte output **identical** to prior writer; checksums, row counts, zip packaging semantics unchanged. |
| `docs/todo/archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **Custom-Code → Spark-Native Execution Parity (config-cascade and write-protocol alignment).** Audit-confirmed two findings against the platform conventions. Finding 2 = same-path overwrite DAG hazard unchanged in Spark 4.x: direct `mode("overwrite").parquet(target_path)` will delete input files mid-DAG-recompute for models that query the same canonical table they write back to; platform-native staging-swap pattern implemented (Track B: `sql/_staging_swap.py` scheme-dispatched atomic swap — POSIX rename + S3 CopyObject/DeleteObject — validate_swap_scheme guard, staging layout, 3 new `SqlRuntimeErrorCode`s). Finding 3 = normalize L1→L2 relationalizer is a pure-Python driver walk over every dict/list/row instance (`normalize/runner.py`, 0 PySpark imports). Spark-native path implemented (Track A: `normalize/planner.py` `StructType` metadata-walk → `NormalizationPlan` → `normalize/spark_runner.py` `posexplode_outer`/struct-flatten + `uuid()` FK chain). Dual-engine gate `NormalizeEngine = Literal["python", "spark"]` with `normalize_engine="python"` default behind OD-3; `mapping_version` hashes, table physical names, column layouts, and L2 manifest layout are byte-identical between engines (Contracts C1–C3 verified). **Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates COMPLETED 2026-08-15.** Non-JVM regression: 170 passed, 30 errors (all JVM-required). **Gate 5 cutover (row-level parity + delete Python engine): NOW COMPLETED + ARCHIVED 2026-08-15. See `archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md`.** |
| `docs/todo/archive/TODO_STORAGE_URI_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **URI-Aware Storage Root Paths + Explicit-Config I/O Dispatch.** Cross-stage correction: sharp explicit string-URI roots, scheme-as-single-routing-key, zero `pathlib` used on root joins, full-URI level-to-level handoffs, EMR-native with no mounts. Aligns the storage contract in `elt_pipeline` per [PRD 08](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/08-prd-storage-root-uri-io-dispatch.md). **Gates 0–4 COMPLETED 2026-08-13.** Gated rollout: path_utils core → CLI typing → storage/connector → sql/publish → hardening → E2E on EMR. Phase 6 / Gate 5 items (full-Spark-test-suite PASS with JDK 17+, EMR no-mounts end-to-end run) **pending execution in suitable environment** (JVM 17+ workstation now available; EMR run still outstanding). No active code changes required; these are verification-only. |
| `docs/todo/archive/TODO_PATHING_COMPLETED.md` | Completed backlog | Archived / complete | **Path/partition/table-identity management post-Spark migration.** Phases 0–5 all COMPLETED 2026-08-11 → 2026-08-13. Covers: P1 lineage column linchpin, P5 environment-stripped paths, L2 parent-directory partition discovery, L3/L4 default partition convention (multi-source side-by-side re-co-location + late-arrival repartitioning by design), and Phase-5 ergonomics (`source.*` token namespace, canonical example, runbook sections). Final design record + implementation reference. Prior descoped item for object-storage URI portability is **IMPLEMENTED** per PRD 08 → see archived `TODO_STORAGE_URI_COMPLETED.md` above. |
| `docs/todo/archive/TODO_SPARK_COMPLETED.md` | Completed backlog | Archived / complete | **Spark-only engine correction backlog.** Replaced sqlite with Spark parquet across L2–L5, added L2→L3 SQL source bridging, examples + CI updated. Final completed snapshot with descoped-items record. The "object storage URIs for level2+" historically-descoped bullet has been annotated *DESCISION SUPERSEDED / SUBSEQUENTLY IMPLEMENTED AND COMPLETE* pointing to the storage-URI archived backlog above. |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG.md` | Backlog tracker | Archived / complete | Final working backlog pointer for the approved implementation scope |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG_COMPLETED.md` | Completed snapshot | Archived / complete | Historical completed snapshot of the detailed implementation backlog |
| `docs/todo/archive/IMPLEMENTATION_SOURCE_PROVENANCE.md` | Provenance review | Archived / complete | Review of implementation source lineage, PRD alignment, and baseline inheritance |

## Interpretation

- `Archived / complete` means the document remains useful for history, audit, and handoff context, but it is not an active work queue.
- `Active` means the document is the current working backlog for required corrections or approved new scope.
- If new approved scope is introduced later, create or update the appropriate backlog document and then update this tracker.

## Next Action Rule

- Do not reopen archived backlog items by implication.
- If work stays inside the already approved scope, treat it as maintenance.
- If work expands scope or corrects the target execution model, add or update the active backlog entry here before treating the solution as complete.
