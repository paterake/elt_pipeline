# TODO Tracker

This file is the top-level tracker for backlog and review documents under `docs/todo/`.

Use it to see which backlog documents exist, what each one is for, and whether it is still active.

## Current Status

- Active implementation backlog documents: **2** — L3/L4 Iceberg serving cutover (Phase 1) and L2 Iceberg lake (Phase 2), the serving-layer workstream under [PRD 09](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/09-prd-level3-level4-serving-and-table-format.md). All prior all-Spark data-plane parity items are resolved and archived.
- Archived implementation backlog documents: Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment), URI-aware storage root paths + I/O dispatch, local-first implementation slice, Spark-only engine correction, path/partition management post-Spark migration, **pure-Spark normalize cutover**, and **publish-sink Spark parity resolution** (all COMPLETED or COMPLETED pending environment sign-off).
- **Current workstream (2026-08-15):** **Serving layer — make L2–L4 consumable via Apache Iceberg + a configurable, BI-tool-agnostic query endpoint** ([PRD 09](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/09-prd-level3-level4-serving-and-table-format.md), Accepted 2026-08-15). Rationale: the all-Spark data-plane goal is met, but the platform lands governed Parquet that **nothing can query** — this workstream closes that usability gap. One table format (Iceberg) spans L2–L4; serving engine + catalog are configurable env-dispatched bindings (Trino = portable reference); L1 stays raw, L5 stays static canned files (no catalog/BI). Net custom-code *reduction*: retires `sql/_staging_swap.py` (Iceberg atomic commits) and the L2 discovery glob + `mergeSchema` hack. **Phase 1 = L3/L4 (`TODO_L3_L4_ICEBERG_SERVING.md`, hand to Trae next); Phase 2 = L2 (`TODO_L2_ICEBERG_LAKE.md`, blocked on Phase 1).**
- **Completed milestone (2026-08-15):** **All-Spark platform parity COMPLETE.** No remaining custom-Python data-plane sites. Pure-Spark normalize cutover **Gates C1–C4 ARCHIVED as COMPLETED 2026-08-15**. Gate C4 (publish sink OD-P1) resolved via Option 3 streaming (`toLocalIterator()` replacing `collect()`) + size guardrail (`ELT_PIPELINE_PUBLISH_MAX_ROWS` default 1M rows, fail-fast). `result_df.collect()` removed from last data-plane site at [publish/runtime.py](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/publish/runtime.py); code-commented as accepted sink boundary per OD-P1 (single-file + checksum delivery contract is intrinsic and cannot be Spark-parallelized anyway).
- **Last completed workstream (2026-08-15):** **Publish Sink Spark Parity Resolution (OD-P1).** Replaced driver `result_df.collect()` (full-set heap spike) with `toLocalIterator()` (partition-by-partition streaming) in [runtime.py](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/publish/runtime.py). Added config-driven row-count ceiling (`ELT_PIPELINE_PUBLISH_MAX_ROWS` env var, default 1,000,000) with fail-fast error code `PUBLISH_ROWS_EXCEED_CEILING`. Refactored `_validate_publish_output` to use Spark-side `row_count` (no row iteration needed). Refactored `_write_publish_output` to accept `Iterator[Row]` and return `rows_written`, with a Spark-count-vs-written mismatch sanity guard (`PUBLISH_ROW_COUNT_MISMATCH`). Added explicit sink-boundary code comment per Option 1 documentation. Verified: byte-identical csv/tsv/jsonl output preserved; checksum + row_count + packaging semantics unchanged.
- **Prior completed workstream (2026-08-15):** **Pure-Spark Cutover: Retire the Custom-Python Normalize Engine** → see [archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md). Gates C1–C3 COMPLETED 2026-08-15. Gate C4 (publish sink OD-P1) = now COMPLETED 2026-08-15 (see entry above).
- **Prior completed workstream (2026-08-15):** **Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment)** — Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates + archive move COMPLETED 2026-08-15; Gate 5 environment sign-off pending (now unblocked). Built the Spark-native relationalizer + Mercell/Camelot staging-swap write protocol. See [archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md).

## Backlog Index

| Document | Type | Status | Purpose |
| --- | --- | --- | --- |
| `docs/todo/TODO_L3_L4_ICEBERG_SERVING.md` | **Active implementation backlog** | **Active — Phase 1 (all gates code-complete; fresh independent 7-session re-verification TODAY 2026-08-16: every claimed fix re-confirmed in source + tests GREEN). 2 repo-hygiene gaps CLOSED today: 3 preflight-spike debris files DELETED + gitignore spike-pattern defense-in-depth added. 4 NEW follow-ups added TODAY 2026-08-16 (F-1..F-4 below; all independent of JVM requirement): F-1 = audit / resolve manifest bootstrap drift (36 scalars superseded by singleton), F-2 = strict zero-OS-env lockdown (env reads only in singleton materializer), F-3 = zero-env Trino end-to-end sign-off smoke test (24h setup churn closed), F-4 = clean architecture audit (runners-only at src root, no god files, facade+1-concern-per-file). Current totals: 48/48 Iceberg-only PASS (23 I2 + 25 parity+audit), 229 non-JVM combined PASS, 0 ruff src/ tree errors, VS Code diagnostics empty, `.using("iceberg")` count = exactly 2 (v2 API conformance), `namespace="spark_parquet"` publish/runtime.py literal grep = 0, repo-root clean). Remaining workstation proof runs pending (JDK 17+ required): I3 Trino SELECT probe against L3+L4, I5 parity full run `ops/run_local_demo_iceberg_parity.sh all`, publish Iceberg end-to-end (namespace=iceberg + exports written + serving_endpoint in both audit JSONs), THEN OD-I1 step (a): default flag flip opt-in → opt-out.** | **L3/L4 Iceberg serving layer — make the platform consumable.** Implements [PRD 09](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/09-prd-level3-level4-serving-and-table-format.md). Today L3/L4 land as plain `writer.parquet(...)` — no catalog, no table format — so no BI tool or ad-hoc SQL can query them. Materialize L3/L4 as **Apache Iceberg** in a **pluggable catalog** (Hadoop local · JDBC/REST/Glue), reachable via a **configurable ANSI-SQL JDBC/ODBC serving engine** (**Trino** portable reference · Athena AWS · Spark Thrift · DuckDB) — **BI-tool-agnostic**, no tool hard-wired. Gated: **I1** Iceberg write path (`load_mode`→native ops, behind existing seam) ✅ (v2 API: `.using("iceberg")` × 2 only, no old `write.format(…).save(…)`) → **I2** catalog binding + dispatch (local needs no cloud) ✅ 23/23 tests GREEN → **I3** serving binding + connectivity proof (JDBC client selects L3/L4 via Trino) code-complete (Trino 468 scripted, `fs.hadoop.enabled=true` verified ×2; audit persistence CLOSED: endpoint dict serialized to SQL + Publish permanent audit JSON via `json.dumps(sort_keys=True)` convention matching `partition_values`; CLI stdout symmetry restored for both commands; 8 regression tests lock this) → **I4** retire `sql/_staging_swap.py` (Iceberg atomic commits dissolve the same-path overwrite hazard by construction — custom-code deletion) code-path-bypassed ✅ (0 swap keywords in `_execute_iceberg_write`, publish dual-path P-2 fixed: `grep namespace="spark_parquet"` publish/runtime.py = 0; 3 source-audit tests lock the dual-path pattern) → **I5** re-materialize existing L3/L4 with parity (compare tooling verified on synthetic data: P-1 path-layout fix confirmed, 3/3 sites uniform no-domain, column-reorder invariant match + mismatch detection + JSON roundtrip all GREEN; 9 regression tests lock this; 4 CLI parity tests lock publish-run 8× iceberg flag contracts identical to sql-run). Publish **CLI parity gap CLOSED: 8 `--iceberg-*` flags + `_validate_iceberg_catalog_binding` + `_resolve_iceberg_session_kwargs` identical on both `sql run` × publish run` (23/23 catalog config tests NO REGRESSIONS; 4 NEW flag-contract parity tests today lock choices/defaults/precedence uniformity). TODAY 2026-08-16: Repo hygiene gap closure — `_dbg_iceberg.py` / `_dbg_iceberg2.py` / `_spike_trino_materialize.py` DELETED (3 transient preflight-spike files; the 2 `_dbg_*` files used the pre-fix `_row_id`/`_parent_row_id` collision-hazard naming — stale reference material removed); `.gitignore` adds `_dbg_*.py` + `_spike_*.py` defense-in-depth patterns next to existing `tempCodeRunnerFile.py`. All prior 6-session claimed fixes independently re-verified fresh: `.using("iceberg")` still exactly 2 lines, swap-code still 0 inside `_execute_iceberg_write`, publish still 0 hardcoded spark_parquet namespaces, parity path layout still no-domain uniform at all 3 sites, CLI parity still 8 identical flags with identical choices on both commands. Transforms stay SQL-only; engine stays Spark; config-author contract unchanged. **NEW TODAY: 4 follow-up hygiene/architecture todos written inline in this doc, § F-1..F-4: audit bootstrap shell cascade (F-1) → strict zero-env lockdown (F-2) → zero-env Trino sign-off (F-3); architecture audit F-4 fully independent. See section "Follow-up Hygiene & Architecture Todos (2026-08-16)" for full spec + completion criteria.** Remaining workstation items (JDK 17+ required): Trino `cli --execute` probe against L3+L4 tables, `ops/run_local_demo_iceberg_parity.sh all` full parity run (confirm `artifacts.audit_path` → `context.serving_endpoint` present in both SQL + Publish JSON), then physical deletion of `sql/_staging_swap.py` L3/L4 legacy path + default flag flip from opt-in to opt-out. |
| `docs/todo/TODO_L2_ICEBERG_LAKE.md` | **Active implementation backlog** | **Active — Phase 2 (blocked on Phase 1).** | **L2 Iceberg lake — uniform table format across L2–L4.** Phase 2 of [PRD 09](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/09-prd-level3-level4-serving-and-table-format.md). L2 is the first SQL-consumable layer (thin materialisation of L1); moving it onto **Iceberg** completes the single-format contract and **deletes bespoke L2 code**: the driver-side discovery glob `path_rglob("**/table=…/run_id=*")` + `mergeSchema` hack in [sql/level2_source.py](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/sql/level2_source.py) (parity Finding 5) → replaced by a catalog table lookup + native schema evolution. Reuses all Phase-1 infra (Iceberg runtime, catalog binding, serving). Gated: **L2-1** write path (normalize producer) → **L2-2** read via catalog, delete the glob/`mergeSchema` → **L2-3** migrate + L1→L2→L3 parity → **L2-4** expose L2 to serving as exploration-only (not a BI surface). **Open: OD-L2-1** — model normalize runs as Iceberg snapshots vs. keep `run_id` for `--rerun-run-id`/audit; decide before starting. L1 stays raw; L2 stays source-aligned. |
| `docs/todo/archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md` | **Completed + archived backlog** | **Archived COMPLETED 2026-08-15. Gates C1–C4 DONE.** | **Pure-Spark cutover: retire the custom-Python normalize engine — COMPLETE.** Platform normalize goal achieved: all L1→L2 relationalization is now pure Spark. Execution: **Gate C1** row-level parity on Temurin 17.0.20 — 2/2 row-level tests + 7/7 post-C3 Spark-native parity tests PASS. 2 bugs found & fixed in `spark_runner.py` (array-column projection drop; `posexplode_outer([])` sentinel filter). Full suite: 212 passed, 32 failed (all 32 pre-existing unrelated drift). **Gate C2:** default flipped to `spark`; `--normalize-engine` reversible flag + operator runbook updated. **Gate C3:** `runner.py` and `test_normalize_runner.py` DELETED; `pipeline.py` rewrote to remove `NormalizeEngine` switch, Python branch, `normalize_engine`/`normalization_runner` params; `cli.py` removed the flag and param threading; `level2_storage.py` deleted `_rows_to_dataframe()` + `write_table()` (Python-only path); `tests/test_normalize_engine_parity.py` rewrote from runner-vs-Spark to unconditional Spark-native assertions. Dead-path `grep` on `src/`+`tests/` for `NormalizationRunner|normalize_engine|_rows_to_dataframe|NormalizeEngine` = **0 matches**. **Gate C4 (publish sink OD-P1): RESOLVED 2026-08-15 per [archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md) — Option 3 streaming + size guardrail.** |
| `docs/todo/archive/TODO_PUBLISH_SINK_SPARK_PARITY_COMPLETED.md` | **Completed + archived backlog** | **Archived / COMPLETE 2026-08-15. OD-P1 RESOLVED (Option 3 streaming + guardrail).** | **Publish sink: driver `collect()` → `toLocalIterator()` streaming + size guardrail — RESOLVED + ARCHIVED.** The last remaining custom-Python data-plane site. **Decision (OD-P1): Option 3 combined with Option 1 documentation + size guardrail.** (a) Replaced full-set driver `collect()` with `result_df.toLocalIterator()` (partition-by-partition streaming; eliminates driver-heap spike). (b) Added Spark-side `.count()` pre-check against config-driven ceiling `ELT_PIPELINE_PUBLISH_MAX_ROWS` (default 1,000,000) → fail-fast `PUBLISH_ROWS_EXCEED_CEILING`. (c) Refactored `_validate_publish_output` to use Spark-side `row_count:int` (no row iteration); `_write_publish_output` accepts `Iterator[Row]` and returns `rows_written` with `PUBLISH_ROW_COUNT_MISMATCH` sanity guard. (d) Added accepted-sink-boundary code comment at call site (single-file + checksum delivery is intrinsic; Spark's `df.write.*()` = `part-*` dirs = wrong shape for external-consumer artifact). csv/tsv/jsonl byte output **identical** to prior writer; checksums, row counts, zip packaging semantics unchanged. |
| `docs/todo/archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment).** Audit-confirmed two findings against the developer's `mercell`/`camelot` conventions. Finding 2 = same-path overwrite DAG hazard unchanged in Spark 4.x: direct `mode("overwrite").parquet(target_path)` will delete input files mid-DAG-recompute for models that query the same canonical table they write back to; Mercell/Camelot staging-swap pattern implemented (Track B: `sql/_staging_swap.py` scheme-dispatched atomic swap — POSIX rename + S3 CopyObject/DeleteObject — validate_swap_scheme guard, staging layout, 3 new `SqlRuntimeErrorCode`s). Finding 3 = normalize L1→L2 relationalizer is a pure-Python driver walk over every dict/list/row instance (`normalize/runner.py`, 0 PySpark imports). Spark-native path implemented (Track A: `normalize/planner.py` `StructType` metadata-walk → `NormalizationPlan` → `normalize/spark_runner.py` `posexplode_outer`/struct-flatten + `uuid()` FK chain). Dual-engine gate `NormalizeEngine = Literal["python", "spark"]` with `normalize_engine="python"` default behind OD-3; `mapping_version` hashes, table physical names, column layouts, and L2 manifest layout are byte-identical between engines (Contracts C1–C3 verified). **Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates COMPLETED 2026-08-15.** Non-JVM regression: 170 passed, 30 errors (all JVM-required). **Gate 5 cutover (row-level parity + delete Python engine): NOW COMPLETED + ARCHIVED 2026-08-15. See `archive/TODO_PURE_SPARK_NORMALIZE_CUTOVER_COMPLETED.md`.** |
| `docs/todo/archive/TODO_STORAGE_URI_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **URI-Aware Storage Root Paths + Explicit-Config I/O Dispatch.** Cross-stage correction: sharp explicit string-URI roots, scheme-as-single-routing-key, zero `pathlib` used on root joins, full-URI level-to-level handoffs, EMR-native with no mounts. Aligns the storage contract in `elt_pipeline` to the sibling-repo conventions (`mercell`, `camelot`) per [PRD 08](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/08-prd-storage-root-uri-io-dispatch.md). **Gates 0–4 COMPLETED 2026-08-13.** Gated rollout: path_utils core → CLI typing → storage/connector → sql/publish → hardening → E2E on EMR. Phase 6 / Gate 5 items (full-Spark-test-suite PASS with JDK 17+, EMR no-mounts end-to-end run) **pending execution in suitable environment** (JVM 17+ workstation now available; EMR run still outstanding). No active code changes required; these are verification-only. |
| `docs/todo/archive/TODO_PATHING_COMPLETED.md` | Completed backlog | Archived / complete | **Path/partition/table-identity management post-Spark migration.** Phases 0–5 all COMPLETED 2026-08-11 → 2026-08-13. Covers: P1 lineage column linchpin, P5 environment-stripped paths, L2 parent-directory partition discovery, L3/L4 default partition convention (Mercell re-co-location + Camelot late-arrival repartitioning by design), and Phase-5 ergonomics (`source.*` token namespace, canonical example, runbook sections). Final design record + implementation reference. Prior descoped item for object-storage URI portability is **IMPLEMENTED** per PRD 08 → see archived `TODO_STORAGE_URI_COMPLETED.md` above. |
| `docs/todo/archive/TODO_SPARK_COMPLETED.md` | Completed backlog | Archived / complete | **Spark-only engine correction backlog.** Replaced sqlite with Spark parquet across L2–L5, added L2→L3 SQL source bridging, examples + CI updated. Final completed snapshot with descoped-items record. The "object storage URIs for level2+" historically-descoped bullet has been annotated *DESCISION SUPERSEDED / SUBSEQUENTLY IMPLEMENTED AND COMPLETE* pointing to the storage-URI archived backlog above. |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG.md` | Backlog tracker | Archived / complete | Final working backlog pointer for the approved implementation scope |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG_COMPLETED.md` | Completed snapshot | Archived / complete | Historical completed snapshot of the detailed implementation backlog |
| `docs/todo/archive/IMPLEMENTATION_SOURCE_PROVENANCE.md` | Provenance review | Archived / complete | Review of implementation source lineage, PRD alignment, and baseline inheritance |

## Interpretation

- `Archived / complete` means the document remains useful for history, audit, and handoff context, but it is not an active work queue.
- `Active` means the document is the current working backlog for required corrections or approved new scope.
- If new approved scope is introduced later, create or update the appropriate backlog document and then update this tracker.

## Next Action Rule

- Do not reopen archived backlog items by implication.
- If work stays inside the already approved scope, treat it as maintenance.
- If work expands scope or corrects the target execution model, add or update the active backlog entry here before treating the solution as complete.
