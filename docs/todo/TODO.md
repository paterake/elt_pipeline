# TODO Tracker

This file is the top-level tracker for backlog and review documents under `docs/todo/`.

Use it to see which backlog documents exist, what each one is for, and whether it is still active.

## Current Status

- Active implementation backlog documents: **1** — publish-sink parity decision (under `docs/todo/`, indexed below). Pure-Spark normalize cutover: **Gates C1–C3 COMPLETED 2026-08-15** (see row below).
- Archived implementation backlog documents: Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment), URI-aware storage root paths + I/O dispatch, local-first implementation slice, Spark-only engine correction, path/partition management post-Spark migration, and pure-Spark normalize cutover (all COMPLETED or COMPLETED pending environment sign-off).
- **Current workstream (2026-08-15):** **Publish-sink decision (last remaining custom-Python data-plane site).** Platform goal: all data processing executes in Spark with **no custom-Python data-plane code**, for runtime portability. The pure-Spark normalize cutover just completed (runner.py deleted, Spark is the only normalize engine; 0 references to NormalizationRunner/normalize_engine/_rows_to_dataframe in src/tests). One remaining active backlog doc: resolve the publish-sink `collect()` decision.
- **Last completed workstream (2026-08-15):** **Pure-Spark Cutover: Retire the Custom-Python Normalize Engine** — Gates C1 (row-level parity on Temurin 17.0.20: 2/2 + 7/7 tests green; 2 bugs fixed in `spark_runner.py`: dropped-array-column projection + empty-array sentinel filter) → C2 (default flipped to `spark`, `--normalize-engine` CLI flag, operator runbook updated) → C3 (`runner.py` + dead-path + obsolete parity tests all deleted, 4 files rewritten, `grep` confirms 0 references under `src/` + `tests/`).
- **Prior completed workstream (2026-08-15):** **Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment)** — Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates + archive move COMPLETED 2026-08-15; Gate 5 environment sign-off pending (now unblocked, see above). Built the Spark-native relationalizer + Mercell/Camelot staging-swap write protocol.
- **Prior completed workstream:** **URI-aware storage root paths + I/O dispatch** (Gates 0–4 COMPLETED as of 2026-08-13; Gate 5 environment-verification pending on JVM 17+ workstation + AWS EMR account). Aligns `elt_pipeline` storage contract to Mercell/Camelot conventions.
- Prior deferred scope resolution: the previously descoped item "object storage URIs for level2+" is now **IMPLEMENTED AND COMPLETE in code** per [PRD 08](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/08-prd-storage-root-uri-io-dispatch.md). Gate 5 checklist items (JVM green tests + full EMR E2E) remain to be run on a suitable environment; see archived [TODO_STORAGE_URI_COMPLETED.md](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/todo/archive/TODO_STORAGE_URI_COMPLETED.md) Phase 6 row.
- New metastore / catalog / `elt_pipeline_cfg` changes still require separate PRD review and a new tracker entry.

## Backlog Index

| Document | Type | Status | Purpose |
| --- | --- | --- | --- |
| `docs/todo/TODO_PURE_SPARK_NORMALIZE_CUTOVER.md` | **Completed backlog** | **Gates C1–C3 COMPLETED 2026-08-15; Gate C4 (publish) cross-ref only** | **Pure-Spark cutover: retire the custom-Python normalize engine — DONE.** Platform normalize goal achieved: all L1→L2 relationalization is now pure Spark. Execution: **Gate C1** row-level parity on Temurin 17.0.20 — 2/2 row-level tests + 7/7 post-C3 Spark-native parity tests PASS. 2 bugs found & fixed in `spark_runner.py` (array-column projection drop; `posexplode_outer([])` sentinel filter). Full suite: 212 passed, 32 failed (all 32 pre-existing unrelated drift). **Gate C2:** default flipped to `spark`; `--normalize-engine` reversible flag + operator runbook updated. **Gate C3:** `runner.py` and `test_normalize_runner.py` DELETED; `pipeline.py` rewrote to remove `NormalizeEngine` switch, Python branch, `normalize_engine`/`normalization_runner` params; `cli.py` removed the flag and param threading; `level2_storage.py` deleted `_rows_to_dataframe()` + `write_table()` (Python-only path); `tests/test_normalize_engine_parity.py` rewrote from runner-vs-Spark to unconditional Spark-native assertions. Dead-path `grep` on `src/`+`tests/` for `NormalizationRunner|normalize_engine|_rows_to_dataframe|NormalizeEngine` = **0 matches**. |
| `docs/todo/TODO_PUBLISH_SINK_SPARK_PARITY.md` | **Active decision item** | **Active (OD-P1: open)** | **Publish sink: driver `collect()` + row-by-row Python write → decision.** The last remaining custom-Python data-plane site: [publish/runtime.py:392](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/src/elt_pipeline/publish/runtime.py#L392) collects the full L4 result to the driver and serializes csv/tsv/jsonl row-by-row in Python. Framed as a decision, not an automatic rewrite, because the single-file + checksum + packaging delivery contract is intrinsic to a publish artifact and Spark's native writers emit `part-*` dirs. Options: (1) accept as a documented sink boundary + size guardrail *(default)*; (2) `coalesce(1)` + rename; (3) `toLocalIterator()` to remove the driver-heap spike. OD-P1 needs the expected max publish output size to decide. |
| `docs/todo/archive/TODO_CUSTOM_CODE_PYTHON_SPARK_PARITY_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **Custom-Code → Spark-Native Execution Parity (Mercell/Camelot Alignment).** Audit-confirmed two findings against the developer's `mercell`/`camelot` conventions. Finding 2 = same-path overwrite DAG hazard unchanged in Spark 4.x: direct `mode("overwrite").parquet(target_path)` will delete input files mid-DAG-recompute for models that query the same canonical table they write back to; Mercell/Camelot staging-swap pattern implemented (Track B: `sql/_staging_swap.py` scheme-dispatched atomic swap — POSIX rename + S3 CopyObject/DeleteObject — validate_swap_scheme guard, staging layout, 3 new `SqlRuntimeErrorCode`s). Finding 3 = normalize L1→L2 relationalizer is a pure-Python driver walk over every dict/list/row instance (`normalize/runner.py`, 0 PySpark imports). Spark-native path implemented (Track A: `normalize/planner.py` `StructType` metadata-walk → `NormalizationPlan` → `normalize/spark_runner.py` `posexplode_outer`/struct-flatten + `uuid()` FK chain). Dual-engine gate `NormalizeEngine = Literal["python", "spark"]` with `normalize_engine="python"` default behind OD-3; `mapping_version` hashes, table physical names, column layouts, and L2 manifest layout are byte-identical between engines (Contracts C1–C3 verified). **Gates 1–4 COMPLETED 2026-08-14; runbook + troubleshooting updates COMPLETED 2026-08-15.** Non-JVM regression: 170 passed, 30 errors (all JVM-required). **Gate 5 (env-only, now unblocked by the JVM 17 workstation): the row-level cutover + engine deletion is tracked forward in the active `TODO_PURE_SPARK_NORMALIZE_CUTOVER.md`.** |
| `docs/todo/archive/TODO_STORAGE_URI_COMPLETED.md` | Completed (Gate 5 pending env) | **Archived / Code-Complete; Signoff Pending** | **URI-Aware Storage Root Paths + Explicit-Config I/O Dispatch.** Cross-stage correction: sharp explicit string-URI roots, scheme-as-single-routing-key, zero `pathlib` used on root joins, full-URI level-to-level handoffs, EMR-native with no mounts. Aligns the storage contract in `elt_pipeline` to the sibling-repo conventions (`mercell`, `camelot`) per [PRD 08](file:///Users/Rakesh.Patel/Documents/__code/git/emailrak/elt_pipeline/docs/prd/08-prd-storage-root-uri-io-dispatch.md). **Gates 0–4 COMPLETED 2026-08-13.** Gated rollout: path_utils core → CLI typing → storage/connector → sql/publish → hardening → E2E on EMR. Phase 6 / Gate 5 items (full-Spark-test-suite PASS with JDK 17+, EMR no-mounts end-to-end run) **pending execution in suitable environment** (JVM 17+ workstation now available; EMR run still outstanding). No active code changes required; these are verification-only. |
| `docs/todo/archive/TODO_PATHING_COMPLETED.md` | Completed backlog | Archived / complete | **Path/partition/table-identity management post-Spark migration.** Phases 0–5 all COMPLETED 2026-08-11 → 2026-08-13. Covers: P1 lineage column linchpin, P5 environment-stripped paths, L2 parent-directory partition discovery, L3/L4 default partition convention (Mercell re-co-location + Camelot late-arrival repartitioning by design), and Phase-5 ergonomics (`source.*` token namespace, canonical example, runbook sections). Final design record + implementation reference. Prior descoped item for object-storage URI portability is **IMPLEMENTED** per PRD 08 → see archived `TODO_STORAGE_URI_COMPLETED.md` above. |
| `docs/todo/archive/TODO_SPARK_COMPLETED.md` | Completed backlog | Archived / complete | **Spark-only engine correction backlog.** Replaced sqlite with Spark parquet across L2–L5, added L2→L3 SQL source bridging, examples + CI updated. Final completed snapshot with descoped-items record. The "object storage URIs for level2+" historically-descoped bullet has been annotated *DESCISION SUPERSEDED / SUBSEQUENTLY IMPLEMENTED AND COMPLETE* pointing to the storage-URI archived backlog above. |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG.md` | Backlog tracker | Archived / complete | Final working backlog pointer for the approved implementation scope |
| `docs/todo/archive/IMPLEMENTATION_BACKLOG_COMPLETED.md` | Completed snapshot | Archived / complete | Historical completed snapshot of the detailed implementation backlog |
| `docs/todo/archive/IMPLEMENTATION_SOURCE_PROVENANCE.md` | Provenance review | Archived / complete | Review of implementation source lineage, PRD alignment, and baseline inheritance |

## Interpretation

- `Archived / complete` means the document remains useful for history, audit, and handoff context, but it is not an active work queue.
- `Active` means the document is the current working backlog for required corrections or approved new scope.
- If new approved scope is introduced later, create or update the appropriate backlog document and then update this tracker.

## Next Action Rule

- Do not reopen archived backlog items by implication.
- If work stays inside the already approved scope, treat it as maintenance.
- If work expands scope or corrects the target execution model, add or update the active backlog entry here before treating the solution as complete.
