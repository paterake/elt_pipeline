# Spark Correction Backlog (Archived)

The Spark correction backlog is complete and has been archived.

- Completed snapshot: `docs/todo/archive/TODO_SPARK_COMPLETED.md`

Next work:

- stay within scope for bug fixes, hardening, and documentation corrections, or
- add/expand a PRD first (e.g. object storage URIs for `level2+`, a metastore, `elt_pipeline_cfg` wiring), then update the backlog, then implement.
